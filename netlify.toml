# netlify.toml
# This file tells Netlify where to find the
# secure serverless functions (like create-payment.js)

[build]
  functions = "netlify/functions"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
